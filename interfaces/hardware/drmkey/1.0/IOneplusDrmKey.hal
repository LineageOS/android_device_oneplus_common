package vendor.oneplus.hardware.drmkey@1.0;

interface IOneplusDrmKey {
    installKeybox(string keybox);
    verifyKeybox();
    sendCommand(string command, string value) generates (int32_t status, string value);
};
